var ReactAuth = require('../common/ReactAuth'),
    Settings;

Settings = ReactAuth.createClass({
    render: function () {
        return (
            <div>Настройки</div>
        )
    }
});

module.exports = Settings;