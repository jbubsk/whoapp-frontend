var ReactAuth = require('../common/ReactAuth');

var Home = ReactAuth.createClass({

    render: function() {
        return (
            <div>Welcome to application</div>
        );
    }

});

module.exports = Home;
